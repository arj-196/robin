[project]
name = "robin-notion-ability"
version = "0.1.0"
description = "Robin Notion connector ability."
requires-python = ">=3.10"
dependencies = [
  "typer>=0.20.0,<1.0.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
