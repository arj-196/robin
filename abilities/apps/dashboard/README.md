# Dashboard Ability

This ability exposes the Robin dashboard directly as a CLI.

## Commands

```bash
./bin/dashboard help
./bin/dashboard status
./bin/dashboard serve
```

## Environment

```bash
DASHBOARD_PORT=3000
```

## Notes

- `serve` starts the local HTTP server.
- `status` prints the current dashboard runtime payload.
