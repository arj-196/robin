[project]
name = "robin-auto-coder-service"
version = "0.1.0"
description = "Robin autonomous coding service."
requires-python = ">=3.10"
dependencies = [
  "loguru>=0.7.3,<1.0.0",
  "typer>=0.20.0,<1.0.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
